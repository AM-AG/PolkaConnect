🎯 Goal

Add a /community page that connects Polkadot users together through:

Leaderboard & reputation system

Active governance participants

Community staking pools

User badges and on-chain achievements

Cross-chain activity analytics

🗂️ Folder & File Setup
/app
 ├── community/
 │    ├── page.tsx
 │    ├── components/
 │    │    ├── Leaderboard.tsx
 │    │    ├── ActivityFeed.tsx
 │    │    ├── PoolCard.tsx
 │    │    └── CommunityStats.tsx
 │    └── data/
 │         └── mockCommunity.json

⚙️ 1️⃣ Community Page Layout

File: /app/community/page.tsx

"use client";

import { Leaderboard } from "./components/Leaderboard";
import { ActivityFeed } from "./components/ActivityFeed";
import { CommunityStats } from "./components/CommunityStats";
import { Card, CardContent, CardHeader, CardTitle } from "@/components/ui/card";

export default function CommunityPage() {
  return (
    <div className="space-y-8">
      <div>
        <h1 className="text-4xl font-bold mb-2">Community</h1>
        <p className="text-muted-foreground">
          Explore top contributors, staking pools, and on-chain community activity.
        </p>
      </div>

      <CommunityStats />

      <div className="grid gap-6 lg:grid-cols-2">
        <Leaderboard />
        <ActivityFeed />
      </div>
    </div>
  );
}

🏆 2️⃣ Leaderboard Component

File: /app/community/components/Leaderboard.tsx

import { Card, CardHeader, CardTitle, CardContent } from "@/components/ui/card";
import { Avatar, AvatarFallback } from "@/components/ui/avatar";
import { Trophy } from "lucide-react";
import mock from "../data/mockCommunity.json";

export function Leaderboard() {
  return (
    <Card>
      <CardHeader className="flex items-center justify-between">
        <CardTitle>Top Community Members</CardTitle>
        <Trophy className="text-yellow-500" />
      </CardHeader>
      <CardContent className="space-y-3">
        {mock.leaderboard.map((member, i) => (
          <div key={i} className="flex items-center justify-between">
            <div className="flex items-center gap-3">
              <Avatar>
                <AvatarFallback>{member.name[0]}</AvatarFallback>
              </Avatar>
              <div>
                <div className="font-medium">{member.name}</div>
                <div className="text-xs text-muted-foreground">{member.address}</div>
              </div>
            </div>
            <span className="font-mono text-sm text-green-600 dark:text-green-400">
              +{member.score} XP
            </span>
          </div>
        ))}
      </CardContent>
    </Card>
  );
}

💬 3️⃣ Activity Feed (On-Chain Events)

File: /app/community/components/ActivityFeed.tsx

"use client";

import { Card, CardHeader, CardTitle, CardContent } from "@/components/ui/card";
import { useEffect, useState } from "react";
import { ApiPromise, WsProvider } from "@polkadot/api";

export function ActivityFeed() {
  const [events, setEvents] = useState<string[]>([]);

  useEffect(() => {
    async function subscribeEvents() {
      const provider = new WsProvider("wss://rpc.polkadot.io");
      const api = await ApiPromise.create({ provider });

      await api.query.system.events((records) => {
        const latest = records
          .slice(-5)
          .map((e) => `${e.event.section}.${e.event.method}`);
        setEvents(latest);
      });
    }
    subscribeEvents();
  }, []);

  return (
    <Card>
      <CardHeader><CardTitle>Live Community Activity</CardTitle></CardHeader>
      <CardContent className="font-mono text-sm space-y-2">
        {events.map((e, i) => (
          <div key={i}>🔹 {e}</div>
        ))}
      </CardContent>
    </Card>
  );
}

📊 4️⃣ Community Stats Overview

File: /app/community/components/CommunityStats.tsx

import { Card, CardHeader, CardTitle, CardContent } from "@/components/ui/card";
import { Users, Coins, GitBranch } from "lucide-react";

export function CommunityStats() {
  const stats = [
    { title: "Active Members", value: "12,482", icon: <Users className="h-4 w-4" /> },
    { title: "Total Staked DOT", value: "1.45M", icon: <Coins className="h-4 w-4" /> },
    { title: "Cross-Chain Channels", value: "33", icon: <GitBranch className="h-4 w-4" /> },
  ];

  return (
    <div className="grid gap-4 md:grid-cols-3">
      {stats.map((s, i) => (
        <Card key={i}>
          <CardHeader className="flex items-center justify-between">
            <CardTitle className="text-sm text-muted-foreground">{s.title}</CardTitle>
            {s.icon}
          </CardHeader>
          <CardContent className="text-2xl font-bold font-mono">{s.value}</CardContent>
        </Card>
      ))}
    </div>
  );
}

🧩 5️⃣ Data File

File: /app/community/data/mockCommunity.json

{
  "leaderboard": [
    { "name": "Alice", "address": "1a2b...3c4d", "score": 1200 },
    { "name": "Bob", "address": "2f4a...1d3e", "score": 1120 },
    { "name": "Charlie", "address": "9e8b...7f1c", "score": 1030 }
  ]
}


Later, replace mock data with:

Polkadot identity queries

Account staking metrics

Governance participation count

🌐 6️⃣ Backend Integration Ideas

Agents should:

Fetch on-chain participation data (referenda, staking, XCM transactions)

Store reputation points in your database

Build XP System:

+10 XP for governance vote

+5 XP for staking reward claim

+20 XP for XCM transfer

🧠 7️⃣ Bonus: Innovative Community Features
Feature	Description
Badges & NFT Avatars	Mint NFTs to recognize governance participation or top stakers.
Community Chat (Matrix / XMTP)	Integrate decentralized chat for cross-chain communication.
DAO Voting Portal	Allow on-chain feature proposals directly from the dashboard.
Collective Pools	Create shared staking pools that auto-distribute rewards.
PolkaEvents Calendar	Display live governance proposal deadlines and validator updates.
✅ Agent Checklist
Task	Description	Status
Create /community route	Add new Next.js page	☐
Build Leaderboard	Use mock data → on-chain later	☐
Add ActivityFeed	Subscribe to system.events	☐
Add Stats Overview	Polkadot API metrics	☐
Connect XP / Reputation	Database + identity	☐
Add chat + DAO portal (bonus)	Optional upgrade	☐
🏁 Result

Your Community Page will:

Show real-time network activity

Highlight top community members

Display live staking and governance stats

Allow cross-chain participation tracking

Integrate with the Polkadot ecosystem natively